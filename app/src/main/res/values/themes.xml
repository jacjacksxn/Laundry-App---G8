<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="android:windowBackground">@color/bg_main</item>
        <item name="colorSurface">@color/bg_surface</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="GlassCard" parent="">
        <item name="android:background">@drawable/glass_background</item>
    </style>

    <style name="ExpandedTitle" parent="TextAppearance.Material3.HeadlineLarge">
        <item name="android:textColor">@color/bg_surface</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>